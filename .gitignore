.venv/
__pycache__/
*.pyc

# recorded drives stay local — they're personal telemetry
logs/

# Car Scanner captures stay local too. A sample one used to ship so that a
# fresh clone could replay something immediately; it was borrowed telemetry
# from before the gauge could record, and it has been cleared out now that
# there are real drives. With captures/ empty, `--replay` falls back to the
# newest drive in logs/.
captures/*

# firmware dumps pulled off the ESP32 — large binaries, and they carry
# whatever NVS secrets (wifi creds, pairing keys) were on the device
backups/

# firmware build artefacts
firmware/build/
# clangd's index, created by the editor when it parses the IDF compile flags
firmware/.cache/
firmware/sdkconfig
firmware/test/host/build/

# fetched by the IDF component manager; dependencies.lock pins the versions
firmware/managed_components/

# extracted from mx5gauge/web/boot.mp4 by tools/build_boot_asset.sh
build-assets/

# WiFi passwords, compiled in. Template: firmware/main/wifi_creds.h.example
firmware/main/wifi_creds.h
